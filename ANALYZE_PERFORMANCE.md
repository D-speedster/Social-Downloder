# 📊 راهنمای آنالیز عملکرد ربات

## دستورات برای دریافت لاگ‌های مهم از سرور

### 1️⃣ آمار کلی (خلاصه)
```bash
cd ~/Social-Downloder

# تعداد کاربران جدید امروز
grep "New user.*registering" logs/start_main.log | grep "$(date +%Y-%m-%d)" | wc -l

# تعداد درخواست‌های موفق (آخرین ساعت)
grep "Total:" logs/youtube_callback.log | tail -n 20

# سرعت دانلود YouTube (آخرین 10 مورد)
grep "MB/s" logs/youtube_downloader.log | tail -n 10
```

### 2️⃣ عملکرد YouTube (دقیق)
```bash
# آخرین 30 دانلود با زمان و سرعت
tail -n 100 logs/youtube_callback.log | grep "Total:"

# مثال خروجی:
# 🎯 Total: 54.76s (DL: 47.79s, UL: 6.63s)
```

### 3️⃣ خطاها (اگر وجود دارد)
```bash
# خطاهای مهم امروز
grep -i "error\|exception" logs/bot.log | grep "$(date +%Y-%m-%d)" | tail -n 20

# خطاهای دانلود
grep "Download/Upload error" logs/youtube_callback.log | tail -n 10
```

### 4️⃣ وضعیت سیستم
```bash
# استفاده از حافظه
free -h

# استفاده از CPU
top -bn1 | grep "Cpu(s)"

# فضای دیسک
df -h | grep "/$"

# پروسه ربات
ps aux | grep python | grep -v grep
```

### 5️⃣ Health Monitor
```bash
# آخرین گزارش سلامت
tail -n 50 logs/health_monitor.log

# هشدارهای ارسال شده
grep "Alert sent" logs/health_monitor.log | tail -n 10
```

---

## دستور یکجا (همه چیز)

```bash
cd ~/Social-Downloder

echo "📊 گزارش عملکرد ربات"
echo "======================"
echo ""

echo "👥 کاربران جدید امروز:"
grep "New user.*registering" logs/start_main.log | grep "$(date +%Y-%m-%d)" | wc -l

echo ""
echo "📈 آخرین 10 درخواست YouTube:"
tail -n 50 logs/youtube_callback.log | grep "Total:" | tail -n 10

echo ""
echo "⚡ سرعت دانلود (آخرین 5 مورد):"
grep "MB/s" logs/youtube_downloader.log | tail -n 5

echo ""
echo "❌ خطاهای امروز:"
grep -i "error" logs/bot.log | grep "$(date +%Y-%m-%d)" | wc -l

echo ""
echo "💾 وضعیت سیستم:"
free -h | grep "Mem:"
df -h | grep "/$"

echo ""
echo "✅ تمام!"
```

---

## نحوه ارسال برای آنالیز

### روش 1: کپی مستقیم
```bash
# اجرای دستور یکجا و کپی خروجی
cd ~/Social-Downloder
bash -c 'دستور بالا'
```

سپس خروجی را کپی کنید و برای من بفرستید.

### روش 2: ذخیره در فایل
```bash
cd ~/Social-Downloder

# ذخیره گزارش در فایل
cat > performance_report.txt << 'EOF'
=== گزارش عملکرد ربات ===
تاریخ: $(date)

1. کاربران جدید امروز:
$(grep "New user.*registering" logs/start_main.log | grep "$(date +%Y-%m-%d)" | wc -l)

2. آخرین 10 درخواست:
$(tail -n 50 logs/youtube_callback.log | grep "Total:" | tail -n 10)

3. سرعت دانلود:
$(grep "MB/s" logs/youtube_downloader.log | tail -n 5)

4. خطاها:
$(grep -i "error" logs/bot.log | grep "$(date +%Y-%m-%d)" | tail -n 10)

5. وضعیت سیستم:
$(free -h | grep "Mem:")
$(df -h | grep "/$")
EOF

# نمایش فایل
cat performance_report.txt
```

---

## اطلاعات مهم برای آنالیز

لطفاً این موارد را برای من بفرستید:

### ✅ حتماً:
1. **تعداد کاربران جدید امروز**
2. **آخرین 10-20 درخواست YouTube** (با زمان DL و UL)
3. **سرعت دانلود** (آخرین 5-10 مورد)
4. **تعداد خطاها** (اگر وجود دارد)

### 📊 اختیاری:
5. استفاده از حافظه و CPU
6. فضای دیسک
7. لاگ Health Monitor

---

## مثال خروجی خوب

```
📊 گزارش عملکرد ربات
======================

👥 کاربران جدید امروز: 50

📈 آخرین 10 درخواست YouTube:
🎯 Total: 54.76s (DL: 47.79s, UL: 6.63s)
🎯 Total: 36.05s (DL: 29.53s, UL: 6.16s)
🎯 Total: 73.61s (DL: 66.41s, UL: 6.85s)
...

⚡ سرعت دانلود (آخرین 5 مورد):
✅ Download completed: 36.37 MB in 47.79s (0.76 MB/s)
✅ Download completed: 6.00 MB in 29.53s (0.20 MB/s)
...

❌ خطاهای امروز: 2

💾 وضعیت سیستم:
Mem: 3.7Gi total, 0.2Gi free, 3.5Gi used
/dev/sda1: 37G total, 30G free (81% free)
```

---

## بعد از دریافت لاگ‌ها

من این موارد را آنالیز می‌کنم:

✅ **عملکرد کلی**: آیا ربات خوب کار کرده؟
✅ **سرعت**: آیا بهینه‌سازی‌ها تأثیر داشته؟
✅ **خطاها**: آیا مشکلی وجود دارد؟
✅ **منابع**: آیا حافظه/CPU کافی است؟
✅ **پیشنهادات**: چه بهبودهایی نیاز است؟

آماده برای دریافت لاگ‌ها! 📊
